# Databricks notebook source
# MAGIC %md
# MAGIC # 00 — Setup (WP0)
# MAGIC Creates catalog `mktpulse`, schemas (`bronze`/`silver`/`gold`/`ops`) and the
# MAGIC `raw` Volume. Seeds the historical raw dataset and the bronze/silver fixtures.
# MAGIC
# MAGIC **Acceptance:** runs clean on a fresh Free Edition workspace; fixtures load;
# MAGIC contracts (see `CONTRACTS.md`) documented.

# COMMAND ----------
# TODO(WP0): implement catalog/schema/volume creation + fixture seeding.
